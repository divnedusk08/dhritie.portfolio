// This AI flow is no longer used and can be safely deleted.
// Keeping the file with this comment to avoid build errors if accidentally imported elsewhere,
// but ideally, it should be removed from the project structure.

'use server';

/**
 * @fileOverview A biography suggestion AI agent. (No longer in use)
 */

export {}; // Ensures the file is treated as a module
